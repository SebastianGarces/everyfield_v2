# Dependencies
node_modules
.pnpm-store
bun.lock

# Next.js
.next
out

# Production
build
dist

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Testing
coverage

# IDE
.idea
# Ignore local VS Code state, but share the two files that configure the
# project itself — format-on-save is part of how this repo stays formatted
# now that there is no pre-commit hook doing it.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Misc
*.pem

# AI Agents
.codex
scratchpad/
# Playwright MCP browser artifacts (snapshots, console logs, screenshots)
.playwright-mcp/

# Working feature (ephemeral planning workspace)
working-feature
# next-agents-md
.next-docs/

# Sentry Config File
.env.sentry-build-plugin
.neon

# impeccable live-mode runtime state (config.json stays tracked)
.impeccable/live/server.json
.impeccable/live/inject-journal.json
.impeccable/live/roots.json

# Local database backups written by scripts/migrate-wiki-related-sections.ts
/backups
